{{> header}}
<div class="container-fluid">
    <h1>Edit car</h1>
    <table class="table">
        <tr>
            <th>_id</th>
            <th>ubezpieczony</th>
            <th>benzyna</th>
            <th>uszkodzony</th>
            <th>naped4x4</th>
            <th></th>
            <th></th>
        </tr>
        {{#each this}}
        {{#if _id}}
        <form action="updateCar?id={{_id}}" method="get"></form>
        <tr id="{{_id}}">

            <td>{{_id}}</td>
            <td class="{{_id}}">{{ubezpieczony}}</td>
            <td class="{{_id}}">{{benzyna}}</td>
            <td class="{{_id}}">{{uszkodzony}}</td>
            <td class="{{_id}}">{{naped4x4}}</td>
            <td>
                <div class="btn btn-primary" onclick="handle_edit('{{_id}}')">EDIT</div>
            </td>
            <td></td>

        </tr>
        </form>
        {{/if}}
        {{/each}}
    </table>
</div>